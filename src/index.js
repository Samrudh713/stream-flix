import React from "react";
import ReactDOM from "react-dom";

import App from "./App"; // importing app into index.js file

ReactDOM.render(<App />, document.getElementById("root"));
// This is going to trigger the div with the id of root and is going to inject our entire react application right inside of it
